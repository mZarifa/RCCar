About:
This program is written to control a cheap RC car which has motors connected to certain pin values. The files themselves are only compatible with an Arduino Board. Operation is stright forward.